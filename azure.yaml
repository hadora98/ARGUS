name: argus
metadata:
  template: containerapp-python@latest
services:
  backend:
    project: src/containerapp
    language: python
    host: containerapp
  frontend:
    project: frontend
    language: python
    host: containerapp

