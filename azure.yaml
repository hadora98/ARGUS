name: argus
metadata:
  template: containerapp-python@latest
services:
  backend:
    project: src/containerapp
    language: python
    host: containerapp
    docker:
      remoteBuild: true
  frontend:
    project: frontend
    language: python
    host: containerapp
    docker:
      remoteBuild: true
